# ── 依赖 ─────────────────────────────
node_modules/
jspm_packages/
web_modules/
.pnp.*

# ── 构建产物 ─────────────────────────
dist/
out
*.vsix
*.tgz
*.tsbuildinfo

# ── 测试产物与覆盖率 ─────────────────
coverage
*.lcov
.nyc_output
.vscode-test/

# ── 缓存 ─────────────────────────────
.cache/
*.eslintcache
*.stylelintcache

# ── 日志 ─────────────────────────────
logs
*.log
.pnpm-debug.log*

# ── 环境变量（密钥不进版本库）────────
.env
.env.local
.env.*.local

# ── 本工程内部 / 生成文件 ────────────
# playground 为本地调试脚手架，整体忽略；保留 .gitkeep 以便占位
playground/
!.gitkeep
docs/
all_code.txt
.workbuddy/
.codebuddy/
.temp/
